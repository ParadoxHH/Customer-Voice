# Optional configuration for the marketing site and app experiences.
# Set VITE_API_URL so login, registration, and admin tools can reach the Flask backend.
# Set VITE_DIGEST_TOKEN if admins will trigger POST /digest/run directly from the portal.

VITE_API_URL=
VITE_DIGEST_TOKEN=
