# Optional configuration for the marketing site and app shell.
# Set VITE_API_URL when you want the /app shell to call the Flask backend.
# Set VITE_DIGEST_TOKEN if you plan to trigger POST /digest/run from the shell.

VITE_API_URL=
VITE_DIGEST_TOKEN=
